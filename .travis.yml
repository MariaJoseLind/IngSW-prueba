language: node_js
node-js:
-11.10.0
cache: npm

install: 
  -npm ci

script: 
 -npm run build  

deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB-TOKEN
  local_dir: build
  on:
    branch: master